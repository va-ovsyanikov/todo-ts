export interface PostsType {
  id: number,
  title: string,
  body: string
  userId: number
}

export interface TaskType {
  id: number;
  title: string;
}

export interface SelectType {
  value: string;
  title: string;
}

